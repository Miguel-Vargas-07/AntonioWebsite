Fixed. Here's the summary:
What was wrong: Your GitHub Pages was set up to build the site from the root of the master branch, but your actual website files — index.html, about.html, booking.html, the css/ and js/ folders, etc. — were all sitting inside a /docs subfolder instead of the root. The root of master only had Documentation, Instructions, and docs folders, so there was no index.html for GitHub Pages to serve, which is why the deployment wasn't working.
What I fixed: In Settings → Pages, I changed the publishing source folder from / (root) to /docs, keeping the branch as master, and saved it. The settings page now confirms: "Your GitHub Pages site is currently being built from the /docs folder in the master branch."
GitHub should redeploy automatically now that the source is corrected — give it a minute or two, then check https://miguel-vargas-07.github.io/AntonioWebsite/ to confirm it's showing your site properly. If it's still not right after that, the next thing to check would be relative paths inside your HTML/CSS/JS files (e.g., links to css/js assets), since that's the next most common cause of a broken Pages deploy.
